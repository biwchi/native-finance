{
  "name": "finance-tracker",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/backend"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/backend dev",
    "typecheck": "bun run --cwd apps/backend typecheck",
    "test": "bun run --cwd apps/backend test",
    "db:generate": "bun run --cwd apps/backend db:generate",
    "db:migrate": "bun run --cwd apps/backend db:migrate",
    "db:studio": "bun run --cwd apps/backend db:studio"
  }
}

